node_modules/
dist/
*.tsbuildinfo
.DS_Store

# Generated diagrams and code. Everything under docs/ is committed on purpose,
# so the ignore is scoped to the scratch output directory rather than being a
# blanket *.svg / *.png.
/out/

# next
.next/
.vercel/

# env
.env
.env.local

# The browser transcript that produced the handoff. Kept locally for reference,
# deliberately not published — see the Provenance section of the README.
CONVERSATION.md

# Working handoffs are private — they carry Linear links and other project
# internals. HANDOFF.md (the original drop) is intentionally public.
HANDOFF-SESSION.md
CLAUDE.local.md
